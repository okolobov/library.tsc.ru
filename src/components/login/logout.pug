h1 Logout

.alert.alert-danger(ng-if="$ctrl.error") {{$ctrl.error}}

//button.btn.btn-primary(type="button", ng-click="$ctrl.logout()", ng-disabled="!$ctrl.isAuthenticated()") Logout


pre v={{$ctrl.isAuthenticated()}}
